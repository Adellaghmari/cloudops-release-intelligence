export const environment = {
  production: true,
  // Public API Gateway URL (not a secret). Relative /api/v1 is for local ng serve + proxy only.
  apiBase: 'https://8kci5uht3d.execute-api.eu-west-1.amazonaws.com/api/v1',
};
